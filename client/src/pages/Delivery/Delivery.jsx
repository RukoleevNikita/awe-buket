import React from 'react';

import { Container } from '../../components';

import styles from './Delivery.module.scss';

export const Delivery = () => {
    
  return (
    <Container>
      <h1 className={styles.title}>Pege Delivery</h1>
    </Container>
  );
};